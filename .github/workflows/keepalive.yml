name: keepalive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render server to prevent sleep
        run: curl -fsS --max-time 30 https://today-chatbot.onrender.com/ || true
